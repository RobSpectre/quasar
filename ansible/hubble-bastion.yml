---
- hosts: hubble
  become: true
  roles:
    - base-os
    - nginx-secure
  vars:
    open_access: true
  tasks:
    - name: Install Netcat for SSH Proxy Command
      apt: name=netcat state=present
